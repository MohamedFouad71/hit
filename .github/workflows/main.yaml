name: CI

on:
  push:
    branches:
    - main
  pull_request:
    branches:
    - main

jobs:
  codeql-analysis:
    uses: ./.github/workflows/codeql.yaml
    permissions:
      actions: read
      contents: read
      security-events: write

  valgrind:
    uses: ./.github/workflows/valgrind.yaml
